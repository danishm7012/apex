{
  "kind": "collectionType",
  "collectionName": "categories",
  "info": {
    "name": "category",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "unique": true
    },
    "sub_categories": {
      "via": "category",
      "collection": "sub-category"
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true
    },
    "products": {
      "via": "categories",
      "collection": "product"
    }
  }
}
